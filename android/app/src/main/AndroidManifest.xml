<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — VibeStock Scanner
    Declares: CAMERA permission, INTERNET permission, both Activities.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Network access for Rust API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Camera for barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="VibeStock"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VibeStock"
        android:usesCleartextTraffic="true" >

        <!-- Login screen — launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="VibeStock — Login"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Barcode scanner screen -->
        <activity
            android:name=".ScannerActivity"
            android:exported="false"
            android:label="VibeStock Scanner"
            android:screenOrientation="portrait" />

    </application>
</manifest>
